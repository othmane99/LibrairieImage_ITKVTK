CMAKE_MINIMUM_REQUIRED(VERSION 3.12)

PROJECT(PremierITK)

FIND_PACKAGE(ITK REQUIRED)
INCLUDE(${ITK_USE_FILE} )

ADD_EXECUTABLE(Detecteur Detecteur.cpp)

TARGET_LINK_LIBRARIES(Detecteur ${ITK_LIBRARIES} )