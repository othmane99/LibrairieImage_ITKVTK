CMAKE_MINIMUM_REQUIRED(VERSION 3.12)

PROJECT(Cone)
FIND_PACKAGE(VTK REQUIRED)
#INCLUDE(${VTK_USE_FILE} )
ADD_EXECUTABLE(Cone cone_source.cpp cone_source.h)

TARGET_LINK_LIBRARIES( Cone ${VTK_LIBRARIES} )

vtk_module_autoinit(
	TARGETS Cone
	MODULES ${VTK_LIBRARIES}
)